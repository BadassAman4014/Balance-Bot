# Analysis

This folder holds consolidated outputs from tuning and test runs.

## Logs

- `logs/` contains the moved run outputs from the task folders.
- File names encode the source path so repeated runs stay unique.
- Add new logs here instead of leaving them beside the controller source.
